export interface ExamInfoType {
    academic_session_id: number,
    exam_id: number,
    exam_session: string,
    exam_name: string,
    exam_start_date: string,
    exam_end_date: string,
    session: string,
}
